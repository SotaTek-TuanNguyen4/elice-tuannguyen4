type FilesSystem = {
  fileName: string;
  pathName: string;
  rawData: string;
  contentText: string;
  arrayBufferFile: ArrayBuffer;
  isExpand: boolean;
  isFolder: boolean;
  isBinary: boolean;
  isFocus: boolean;
};

export type { FilesSystem };
